language: ruby

rvm:
  - "2.1"
  - "2.0"

bundler_args: "--jobs 4"

before_install:
  - sudo apt-get update -y
  - sudo apt-get install -y libfuse-dev

script:
  - bundle exec rake test

cache:
  bundler: true
